import 'package:flutter/material.dart';

class SantriPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Santri Page")),
      body: Center(child: Text("Selamat datang, Santri!")),
    );
  }
}
